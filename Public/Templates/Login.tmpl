{{ define "Login" }}
{{ template "Header" }}
<div class="row mt-5">
    <div class="col-6 offset-3">
        <h2>Login</h2>
        <form method="POST" action="authenticate">
            <div class="form-group">
                <label> Username </label>
                <input type="text" name="username" value="Jansen" class="form-control"/>
            </div>
            
            <div class="form-group">
                <label> Password </label>
                <input type="password" name="password"  value="2018-05-21" class="form-control"/>
            </div>
            <input type="submit" value="login" class="btn btn-success"/>
        </form>
    </div>
</div>
{{ template "Footer" }}
{{ end }}